// import { createDarkModeAction } from "../action-creators/darkThemeAction";

// export const handleDarkTheme = (theme) => async (dispatch) => {
//   dispatch(startLoading(theme));
// fetch => success ? error
// };
